# Book settings — https://jupyterbook.org/customize/config.html

title: DesignSafe AI Training
author: Krishna Kumar, Kooshan Amini, and Jamie Ellen Padgett
logo: logo.png
copyright: "2026"

# Render the notebooks' SAVED outputs — do not re-execute on build (the demo needs a
# DesignSafe session, DAPI, and a GPU, which the CI runner doesn't have).
# The this training notebooks are committed already-executed, so their
# figures and printed results come from a real run.
execute:
  execute_notebooks: "off"

# The per-module README.md files are GitHub-facing documentation, not book
# chapters; without this they raise "document isn't included in any toctree".
exclude_patterns:
  - 01-opensees-ml/README.md
  - 02-mlp/README.md
  - 03-xai/README.md
  - 04-pinn/README.md
  - 05-operator-learning/README.md
  - 06-clipseg/README.md
  - 06-clipseg/utils/README.md
  - 06-clipseg/weights/README.md
  - CHANGELOG.md
  - PREVIEW.md
  - LICENSE.md
  - _build

# The notebooks embed figures with HTML <img src="figs/..."> tags, because that
# form renders natively on GitHub and in JupyterLab. Sphinx would otherwise pass
# those tags through verbatim and never copy the files into the build, so every
# figure 404s on the published site. `html_image` parses them into real Sphinx
# image nodes, which copies the file and rewrites the src.
# NOTE: setting this list replaces Jupyter Book's defaults, so the defaults we
# rely on (dollarmath for $...$ maths, colon_fence, etc.) must be listed too.
parse:
  myst_enable_extensions:
    - amsmath
    - colon_fence
    - deflist
    - dollarmath
    - html_image
    - linkify
    - substitution
    - tasklist

sphinx:
  config:
    # Generate GitHub-style anchors for headings so the section links in
    # README.md resolve both on GitHub and in the built book.
    myst_heading_anchors: 3

launch_buttons:
  notebook_interface: "jupyterlab"

# Where the book lives on the web (for the repository / issue buttons).
repository:
  url: https://github.com/DesignSafe-CI/training-ai
  branch: main

html:
  use_issues_button: true
  use_repository_button: true
  extra_navbar: ""
